# Rebase Practice (Branch rebase-a)

Instructions:
1. Checkout `rebase-b`: `git checkout rebase-b`
2. Run `git rebase rebase-a`
3. Since the branches touch different files, the rebase should complete without conflicts.
4. Verify that both `rebase-a.txt` and `rebase-b.txt` exist after the rebase.
